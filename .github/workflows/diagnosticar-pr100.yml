name: Diagnosticar PR 100

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  diagnosticar:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    services:
      mysql:
        image: mysql:8.4
        env:
          MYSQL_DATABASE: estiba_wms_test
          MYSQL_ROOT_PASSWORD: root
        ports:
          - 3307:3306
        options: >-
          --health-cmd="mysqladmin ping --silent"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=10
    env:
      APP_ENV: testing
      APP_KEY: base64:AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=
      CACHE_STORE: array
      DB_CONNECTION: mysql
      DB_HOST: 127.0.0.1
      DB_PORT: 3307
      DB_DATABASE: estiba_wms_test
      DB_USERNAME: root
      DB_PASSWORD: root
      QUEUE_CONNECTION: sync
      SESSION_DRIVER: array
    steps:
      - name: Descargar rama
        uses: actions/checkout@v7
        with:
          ref: agent/navegacion-por-dominios
          fetch-depth: 0

      - name: Preparar PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          extensions: mbstring, pdo_mysql, zip
          coverage: none

      - name: Instalar dependencias
        run: composer install --no-interaction --prefer-dist --no-progress

      - name: Ejecutar suite y guardar resumen
        shell: bash
        run: |
          cp .env.example .env
          set +e
          php artisan migrate:fresh --force > /tmp/migracion.log 2>&1
          php artisan test --fail-on-warning > /tmp/tests.log 2>&1
          codigo=$?
          {
            echo "Código: $codigo"
            echo
            tail -n 220 /tmp/tests.log
          } > tools/diagnostico_pr100.txt
          exit 0

      - name: Publicar diagnóstico
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add tools/diagnostico_pr100.txt
          git commit -m "chore: registrar diagnóstico de pruebas del PR 100" || true
          git push origin HEAD:agent/navegacion-por-dominios
